generated: '2026-08-12'
method: searched
source: https://docs.dustid.io/integrate/ai-agents/
note: >-
  These are NOT generated by API Evangelist. DUST Identity authors and publishes them
  itself, at stable URLs under https://docs.dustid.io/skills/, and both files are saved
  here verbatim. They are in the Agent Skill format — YAML frontmatter carrying name and
  description, then self-contained markdown instructions — and DUST regenerates them at
  every docs build from the documentation content collection and the current OpenAPI
  spec, stamping each with the docs and spec version it came from (2026.7.4 at harvest).
  DUST also documents the install path explicitly, including placing the file at
  .claude/skills/<name>/SKILL.md for Claude Code. A provider publishing versioned,
  spec-derived agent skills alongside llms.txt and a live OpenAPI is a rare posture and
  is the strongest single thing in this profile.
skills:
- file: dust-identity-dice-api-integration.md
  name: dice-api-integration
  version: '2026.7.4'
  source: https://docs.dustid.io/skills/dice-api-integration/SKILL.md
  api: openapi/dust-identity-apid-openapi.yml
  size_bytes: 35812
  description: >-
    Integrate software against the DUST platform API (apid) — authentication with API
    keys and bearer tokens, org/team context headers, and the core flows: creating
    Threads, binding and identifying physical Identifiers, uploading files (tus),
    sharing with other Teams, and sending Shipments.
  covers: [authentication, context headers, request conventions, thread creation,
    identifier binding and identification, file upload via tus, sharing, shipments,
    error handling]
  note: >-
    Contains an endpoint index generated from the OpenAPI spec at build time, so its
    operation list cannot silently drift from the API. Ships both TypeScript-client and
    raw-curl variants of every flow.
- file: dust-identity-dust-go-connect-integration.md
  name: dust-go-connect-integration
  version: '2026.7.4'
  source: https://docs.dustid.io/skills/dust-go-connect-integration/SKILL.md
  api: openapi/dust-identity-apid-openapi.yml
  size_bytes: 10370
  description: >-
    Make a customer web app scan DUST identifiers when it runs inside the DUST Go mobile
    app, using the @dustid/dust-go-connect npm package — detect the app, capture scans
    (promise and listener APIs), handle DUST/QR/barcode/NFC payloads, resolve captures
    server-side against the DUST API, and handle OAuth redirect rewriting inside the
    WebView.
  covers: [DUST Go app detection, scan capture APIs, payload handling, server-side
    resolution against /api/v1/tags/*, OAuth redirect rewriting in WebView, app-link
    allowlist constraints]
  package: '@dustid/dust-go-connect'
install_guidance:
  claude_code: place at .claude/skills/<name>/SKILL.md in the project; discovered
    automatically
  other_agents: include the file in the agent's context or system prompt — plain
    markdown, self-contained
  provider_guidance: >-
    DUST tells integrators to re-download the skills when updating an integration, since
    each file records the docs and OpenAPI versions it was generated from.
summary:
  count: 2
  provider_authored: 2
  api_evangelist_generated: 0
  generated_from_spec: true
  versioned: true
